# Database settings
DB_ROOT_PASSWORD=wordpress_root_password
DB_NAME=wordpress
DB_USER=wordpress_user
DB_PASSWORD=wordpress_password

# WordPress settings
WP_DEBUG=1

# Node.js settings
NODE_ENV=production

# Domain configuration
DOMAIN=localhost

# For production, set to your actual domain
# DOMAIN=your-domain.com

# Staging flag (development/staging/production)
ENVIRONMENT=development

# Other custom settings
# SSL_ENABLED=false
# ENABLE_CACHE=false